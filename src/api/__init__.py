"""Top-level API package initialization.

Ensures imports like `from api.models...` work when tests are run from subdirectories.
"""

# Empty init - models are imported directly from 'models' package at src/models
